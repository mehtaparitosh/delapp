.logo
  = image_tag("logo.png", size: "160x160", alt: "Logo", class: "w3-center")
.links
  %p#buttons
    -if user_signed_in?
      = link_to 'Logout', destroy_user_session_path, method: :delete, class: "btn btn-ghost"
      - if current_user.is_admin == true
        = link_to "Download", entries_path(format: 'xls'), class: "btn btn-ghost"
.nav-menu
  %p#in
    %i.fa.fa-angle-right
    Inventory
  %p#s
    %i.fa.fa-angle-right
    Security Baseline
  -# %p#id Identity and Access Management
  %p#id
    %i.fa.fa-angle-right
    IAM
  %p#v
    %i.fa.fa-angle-right
    Vulnerability Landscape
  %p#m
    %i.fa.fa-angle-right
    Monitoring
